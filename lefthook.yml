pre-commit:
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx,mjs,cjs}"
      run: npx biome check --write --unsafe {staged_files}
      stage_fixed: true
    test:
      glob: "src/**/*.{js,ts,jsx,tsx,mjs,cjs}"
      run: npm test
    typecheck:
      glob: "src/**/*.{js,ts,jsx,tsx,mjs,cjs}"
      run: npx tsc --noEmit --jsx react
